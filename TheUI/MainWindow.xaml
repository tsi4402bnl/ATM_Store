<Page x:Class="TheUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TheUI"
        Background="White"
        Width="500"
        Height="400" Loaded="Page_Loaded"
      >

    <Page.Resources>
        <Style TargetType="ItemsControl" x:Key="LogViewerStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <ScrollViewer CanContentScroll="True">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="White"/>
        </Style>

        <DataTemplate DataType="{x:Type local:LogEntry}">
            <Grid IsSharedSizeScope="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition SharedSizeGroup="Index" Width="Auto"/>
                    <ColumnDefinition SharedSizeGroup="Date" Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding DateTime}" Grid.Column="1"
                       Margin="5,0,5,0" Background="White"/>

                <TextBlock Text="" Grid.Column="2"
                       Margin="0,0,2,0" Background="White"/>

                <TextBlock Text="{Binding Message}" Grid.Column="3"
                       TextWrapping="Wrap" Background="White"/>
            </Grid>
        </DataTemplate>

    </Page.Resources>

    <DockPanel Name="mainPanel">
        <DockPanel DockPanel.Dock="Top">
            <WrapPanel Height="50" VerticalAlignment="Center">
                <Label>Header</Label>
            </WrapPanel>
        </DockPanel>
        <DockPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom" Height="70"><!--set Visibility="Collapsed" to disable LOG-->
            <Button Width="50" Height="20" Content="Clear" Click="Clear_Log" DockPanel.Dock="Bottom" HorizontalAlignment="Right"/>
            <ItemsControl ItemsSource="{Binding}" Style="{StaticResource LogViewerStyle}" >
                <ItemsControl.Template>
                    <ControlTemplate>
                        <ScrollViewer CanContentScroll="True" local:AutoScrollHelper.AutoScroll="True">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </ControlTemplate>
                </ItemsControl.Template>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </DockPanel>
        <DockPanel>
            <TabControl 
                        TabStripPlacement="Left"
                        HorizontalContentAlignment="Left"
                        VerticalContentAlignment="Top"
                        HorizontalAlignment="Stretch"
                        >
                <TabItem Header="TabItem1">
                    <Grid Background="#FCFAFC" >
                        <StackPanel HorizontalAlignment="Left">
                            <Button Name="btnTestTabItem1" 
                                    Click="btnTestTabItem1_Click"
                                    Width="337"
                                    Height="30"
                                    Content="{Binding Path=btnTestTabItem1Text.Value, RelativeSource={RelativeSource AncestorType={x:Type Page}}}"
                                    >
                            </Button>
                        </StackPanel>
                    </Grid>
                </TabItem>
                <TabItem Header="TabItem2">
                    <Grid Margin="10">
                        <DataGrid Name="dgItems" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name.Value}" IsReadOnly="True" />
                                <DataGridTextColumn Header="Price" Binding="{Binding Price.Value}"  IsReadOnly="True" />
                            </DataGrid.Columns>
                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <DockPanel Background="GhostWhite">
                                        <!--<Image DockPanel.Dock="Left" Source="{Binding ImageUrl}" Height="64" Margin="10" />-->
                                        <Grid Margin="0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="ID: " FontWeight="Bold" />
                                            <TextBlock Text="{Binding Id.Value}" Grid.Column="1" />
                                            <TextBlock Text="Name: " FontWeight="Bold" Grid.Row="1" />
                                            <TextBlock Text="{Binding Name.Value}" Grid.Column="1" Grid.Row="1" />
                                            <TextBlock Text="Price: " FontWeight="Bold" Grid.Row="2" />
                                            <TextBox Text="{Binding Price.Value, StringFormat=d}" Grid.Column="1" Grid.Row="2" Width="50" HorizontalAlignment="Left"/>
                                            <Button Grid.Column="2" Grid.Row="2" Width="50" HorizontalAlignment="Left" Click="Button_Click">Save</Button>

                                        </Grid>
                                    </DockPanel>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>
                        </DataGrid>
                    </Grid>
                </TabItem>
            </TabControl>
        </DockPanel>
    </DockPanel>
</Page>
